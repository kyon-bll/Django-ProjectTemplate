from django.urls import path

from . import views

app_name = '{% templatetag openvariable %} app_name {% templatetag closevariable %}'

urlpatterns = [
    # path('path/<str:param>/', views.ViewName.as_view(), name=''),
]
